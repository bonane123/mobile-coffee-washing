import { View, Text } from "react-native";
import React from "react";
import Map from "../../components/Map";

const StationCreateScreen = () => {
  return (
    <View>
      <Map/>
    </View>
  );
};

export default StationCreateScreen;
